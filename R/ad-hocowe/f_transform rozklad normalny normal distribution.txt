s³owa klucze: f_transform rozk³ad normalny geo geomarketing indeksy œrednia indeksu
key words: f_transform normal distribution geo geomarketing indices index mean 


1)	Przy liczeniu indeksów dla geo warto dokonaæ transformacji zmiennych za pomoc¹ poni¿szej funkcji:

f_transform <- function(x){
  f_tmp <- ecdf(x)
  return(list(fun_norm = function(y) 
    mice::squeeze(qnorm(f_tmp(y) - 1/(2*length(x)), 1, 1/3), c(0, 2)), 
    unif = f_tmp(x), normal = mice::squeeze(qnorm(f_tmp(x) - 1/(2*length(x)), 1, 1/3), c(0, 2))))
} 
Powy¿sza funkcja transformuje rozk³ad pierwotny na normalny (bardzo zbli¿ony do niego, bo obcinamy ogony) wyra¿ony na skali 0-2 ze œredni¹ 1.

A nastêpnie liczyæ œrednie, segmentowaæ WISEm (WISe dobrze znosi zmienne o rozk³adzie normalnym) itp.

Celem tej transformacji jest uczynienie zadoœæ potrzebie wyniku w postaci indeksu ze œredni¹ 1.
